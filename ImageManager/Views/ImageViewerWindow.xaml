<Window x:Class="ImageManager.Views.ImageViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageManager.Converters"
        Title="Image Viewer"
        WindowState="Maximized"
        WindowStyle="None"
        Background="Black"
        xmlns:Behaviours="clr-namespace:ImageManager">
    <Window.Resources>
        <local:ImageConverter x:Key="ImageConverter"/>
        <Style x:Key="StyleMainGrid">
            <Style.Triggers>
                <DataTrigger  Binding="{Binding CloseSignal}" Value="true">
                    <Setter Property="Behaviours:WindowCloseBehaviour.Close" Value="true"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Margin" Value="6,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                        CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2196F3"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Storyboard x:Key="FadeTransitionStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1"
                             Duration="0:0:0.5"
                             FillBehavior="HoldEnd"/>
        </Storyboard>
    </Window.Resources>

    <Grid x:Name="RootGrid" Style="{StaticResource StyleMainGrid}">
        <Image x:Name="FullImage"
               Source="{Binding CurrentImage.Path, Converter={StaticResource ImageConverter}}"
               Stretch="Uniform"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Opacity="1"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="20">
            <Button Content="{Binding SlideshowButtonText}"
                    Command="{Binding ToggleSlideshowCommand}"
                    Width="80"
                    Height="40"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="10,0"/>
            <Button Content="Exit"
                    Command="{Binding ExitCommand}"
                    Width="80"
                    Height="40"
                    Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
